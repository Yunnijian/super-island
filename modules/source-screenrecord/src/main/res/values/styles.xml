<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      This activity only brokers the system projection consent. Keeping its window transparent and
      edge-to-edge prevents HyperOS from drawing a default opaque status/navigation bar between
      the caller and MediaProjectionPermissionActivity.
    -->
    <style name="Theme.SuperIsland.ScreenRecording.Capture" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
