<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <permission
        android:name="io.github.superisland.permission.SEND_RESIDENT_ISLAND"
        android:protectionLevel="signature" />
    <permission
        android:name="io.github.superisland.permission.SEND_SCREEN_RECORDING_CONTROL"
        android:protectionLevel="signature" />

    <uses-permission android:name="io.github.superisland.permission.SEND_RESIDENT_ISLAND" />
    <uses-permission android:name="io.github.superisland.permission.SEND_SCREEN_RECORDING_CONTROL" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="com.android.permission.GET_INSTALLED_APPS" />

    <queries>
        <package android:name="com.android.systemui" />
        <package android:name="com.miui.securitycenter" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".SuperIslandApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:description="@string/xposed_module_description"
        android:icon="@drawable/ic_stat_island"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_stat_island"
        android:supportsRtl="true"
        android:theme="@style/Theme.SuperIsland"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>
        <provider
            android:name=".SmartCapsuleReportProvider"
            android:authorities="io.github.superisland.smartcapsule.reports"
            android:exported="true"
            android:grantUriPermissions="false"
            android:permission="android.permission.STATUS_BAR_SERVICE" />

        <receiver
            android:name=".SmartCapsuleXmsfAcceptanceReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.github.superisland.action.REPORT_XMSF_SMART_CAPSULE_ACCEPTANCE" />
            </intent-filter>
        </receiver>
        <service
            android:name=".BatteryMonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!--
                Battery telemetry monitoring has no dedicated Android foreground-service type.
                The service is only started from the visible monitor screen and is always
                user-stoppable from both the app and its ongoing notification.
            -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="user-requested persistent battery telemetry shown through a HyperOS focus notification" />
        </service>
    </application>
</manifest>
