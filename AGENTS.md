# Agent 工作规则

本文件只定义协作与验证边界。产品事实、架构和历史不在这里重复维护。

## 开始前

1. 阅读 `docs/product.md`。
2. 阅读 `docs/status.md`。
3. 阅读与任务相关的 `docs/architecture/` 和 `docs/development/` 文档。
4. 用当前代码、测试和设备证据核对文档，不把归档报告当成现行规范。

## 不可破坏的产品边界

- 产品运行前提是 Root + 已激活 LSPosed。
- 静态 scope 仅包含 SystemUI、XMSF 和隔离的 MiShare scope。
- 第三方通知必须原地增强同一源 SBN；不得 clone、代发、取消或隐藏源通知。
- 常驻岛由 SystemUI 宿主持有；卡片 `contentIntent` 保持为空。
- ColorOS 流体云运行时保持冻结，除非用户重新批准产品方向。PMB110 录屏岛可直接
  复用经审计的 ColorOS 标准 Android 静态资源与视觉参数，但不等于解冻
  LightLiveAlert、Seedling/Pantanal、Oplus Provider 或旧流体云 Hook。
- Miuix 与 Material 共享状态但同屏不混用视觉组件。

## 工作方式

- 先只读确认现状和改动范围，再编辑。
- 手工编辑使用 `apply_patch`；机械格式化或批量路径更新可以使用对应工具。
- 不回滚无法确认来源的用户改动，不使用破坏性 Git 命令。
- 原始证据存放到外部实验目录；仓库只保留脱敏索引和可复现脚本。
- 新功能先评估公开 API；DexKit 只用于有界定位混淆成员。

## 开源上游复用

- 用户要求“对齐”“照搬”“复用”某个开源上游时，该上游对应实现是唯一的行为来源。
  必须先定位固定版本的原始文件和调用链，再直接迁入所需代码；不得用自行设计的等价
  实现、简化版或补丁替代。
- 迁入时只允许进行包名、导入、构建依赖和当前宿主 API 的最小适配。算法、状态机、
  缓存边界、调用顺序、默认值和 UI 行为不得擅自改变。若适配需要改变任一语义，必须先
  向用户说明具体差异并取得同意。
- 不得仅根据功能表面相似就宣称“已复用”或“已对齐”。需要以文件级映射和实际行为验证
  确认；未迁入的上游链路必须明确列出，不能由自写代码隐式替代。
- 对 HyperLyric，用户目前只排除三项：通知型灵动岛 Live Update 本身、
  `UnlockFocusWhitelist` 焦点通知白名单移除实现，以及整个 HyperLyric App 的设置页、
  导航、应用 UI/UX 与页面转场。超级岛现有的 Miuix/Material 页面是唯一设置入口，不能
  迁入、挂载或改造成上游 App 页面。媒体卡片、插件、服务和超级岛内运行时歌词动画与视觉
  效果均在迁入范围，不能被笼统称为“特殊功能”或自行排除。Live Update 的共享依赖若被
  其它已纳入功能使用，仍须迁入；只排除其通知发布链和对应设置入口。
- 用户所说的 HyperLyric“资源完整迁移”指可执行源码，不指上游应用图标、预览图、示例
  封面、贡献者头像或 App 页面动画素材。除超级岛歌词运行时有已记录的不可替代依赖外，
  不得复制这些静态视觉资产；不得因为缺少它们而自行重写核心功能。
- 产品安全边界与静态 scope 仍优先于上游代码；如某个具体上游文件确实冲突，停止迁入该
  文件，说明冲突和影响并等待用户决定，不能借此扩大排除范围。

## 验证

```shell
./scripts/check.sh
```

`docs/development/testing.md` 是强制验收契约。Hook、依赖、R8、性能或运行链变化必须
安装同一次门禁生成的 benchmark APK，并完成其中规定的进程重载、身份核验、日志扫描、
真实行为、双皮肤和证据同步；不能用 Debug APK、旧 APK 或历史证据替代。

编译通过不等于完成。只有真实行为、即时 UI、持久化、返回/动画、失败回退和要求的真机
证据均成立后，才能把对应功能标记为通过；无法自动完成的步骤必须明确保留为手动验收。
